#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

#define TXT 1024
#define WORD 30
int val(char);
char atbash(char);
int valueOfChar(char c);
void Gematria_Sequences(char word[], char text[]);
char *strrevers(char *str);
char *wordToAtbash(char *word);
void Atbash_Sequences(char *word , char *text);
void Anagram_Sequences(char text[], char word[]);
/*#define TXT 1024
#define WORD 30
int val(char);
char atbash(char);
int valueOfChar(char c);
void Gematria_Sequences(char text[], char word[]);
void reverse_the_word(char word[]);
void atbasfunc(char a[], char b[]);
int isAnagram(char* a, char* b);
void Anagram_Sequences(char text[], char word[]);*/



